{
  "name": "faq_title",
  "description": "{{t(faq_title)}} section with ability to add questions and answers",
  "settings": {
    "title": { 
      "type": "string", 
      "label": "{{t(question)}}", 
      "default": "{{t(faq_title)}}" 
    }
  },
  "blocks": {
    "faqItem": {
      "name": "{{t(faq_item)}}",
      "settings": {
        "question": {
          "type": "string",
          "label": "{{t(question)}}",
          "default": "{{t(how_to_return_product)}}"
        },
        "answer": {
          "type": "text",
          "label": "{{t(answer)}}",
          "default": "{{t(return_policy_answer)}}"
        }
      }
    }
  },
  "presets": [
    {
      "category": "{{t(home)}}",
      "name": "⭐ {{t(faq_title)}}",
      "blocks": [
        {
          "block": "faqItem",
          "name": "{{t(first_question)}}",
          "settings": {
            "question": "{{t(how_to_return_product)}}",
            "answer": "{{t(return_policy_answer)}}"
          }
        },
        {
          "block": "faqItem",
          "name": "{{t(second_question)}}",
          "settings": {
            "question": "{{t(shipping_duration_question)}}",
            "answer": "{{t(shipping_duration_answer)}}"
          }
        },
        {
          "block": "faqItem",
          "name": "{{t(third_question)}}",
          "settings": {
            "question": "{{t(free_shipping_question)}}",
            "answer": "{{t(free_shipping_answer)}}"
          }
        }
      ]
    }
  ]
}

