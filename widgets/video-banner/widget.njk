<section class="video-banner-full">
  <div class="video-container">
    <!-- الفيديو كخلفية -->
    <video class="video-background" preload="auto" autoplay muted playsinline loop>
      <source src="{{ widget.data.video_url.fileUrl }}" type="video/mp4">
    </video>
    
    <!-- Overlay داكن -->
    <div class="video-overlay"></div>
    
    <!-- {% if t('dir') == 'rtl' %}النص في الزاوية العلوية اليمنى{% else %}Text in the top right corner{% endif %} -->
    <div class="video-text-banner">
      <h2 class="video-title">
        {{ t('default_title_text') }}
      </h2>
    </div>
    
    <!-- زر التشغيل في المنتصف -->
    <div class="video-play-container">
      <button class="video-play-button">
        <span class="play-icon"></span>
      </button>
    </div>
  </div>
</section>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const video = document.querySelector('.video-background');
  const playButton = document.querySelector('.video-play-button');
  const playIcon = document.querySelector('.play-icon');
  
  if (video && playButton) {
    let isPlaying = true;
    
    playButton.addEventListener('click', function() {
      if (isPlaying) {
        video.pause();
        playIcon.style.borderLeft = '20px solid #F59A57';
        playIcon.style.borderTop = '12px solid transparent';
        playIcon.style.borderBottom = '12px solid transparent';
        isPlaying = false;
      } else {
        video.play();
        playIcon.style.borderLeft = '0';
        playIcon.style.borderTop = '0';
        playIcon.style.borderBottom = '0';
        playIcon.style.border = '2px solid #F59A57';
        playIcon.style.width = '20px';
        playIcon.style.height = '20px';
        playIcon.style.borderRadius = '2px';
        isPlaying = true;
      }
    });
  }
});
</script>
